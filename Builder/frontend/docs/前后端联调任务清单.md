# 前后端联调任务清单

> 创建时间: 2026-03-14
> 更新时间: 2026-03-14
> 目标: 全面替换 Mock 数据，完成真实接口对接

---

## 概述

本文档记录前后端联调的所有任务，按优先级分为 P0（核心功能）、P1（重要优化）和 P2（独立功能模块）。

---
  🎯 **目标**: 全面替换 Mock 数据，完成真实接口对接
  📍 **API文档**: http://localhost:9527/v3/api-docs
  📍 **Swagger**:  http://localhost:9527/swagger-ui/index.html#/

网站登录账号：账户：admin，密码：Admin123

## 严格约束

以下约束为强制性要求，**绝对禁止违反**：

### 目录边界约束

- ❌ **禁止操作当前工作目录以外的任何文件和目录**

- ✅ 所有文件读写操作必须在项目根目录范围内

- ❌ 禁止访问系统敏感目录（如 `/etc`, `C:\Windows`, 用户主目录等）

- ❌ 禁止修改项目外的配置文件、环境变量

- ✅ 每个任务完成必须自测 → 打包验证 → git 提交

## 已完成的 API 对接

| 模块 | 涉及接口 | 状态 |
|------|----------|------|
| 认证模块 | `POST /auth/login`, `POST /auth/register`, `GET /user/profile` | ✅ 已完成 |
| 项目列表 | `GET /projects`, `POST /projects`, `GET /projects/stats` | ✅ 已完成 |
| 项目详情 | `GET /projects/{id}`, `PUT /projects/{id}`, `DELETE /projects/{id}` | ✅ 已完成 |
| 成员管理 | `GET /projects/{id}/members`, `POST /projects/{id}/members`, `DELETE /projects/{id}/members/{userId}` | ✅ 已完成 |

---

## Mock 接口清单与后端接口映射

### 1. 任务看板模块

| Mock 数据 | 所在文件 | 行号 | 对应后端接口 | 接口状态 |
|-----------|----------|------|--------------|----------|
| `initialColumns` | `src/app/(dashboard)/projects/[id]/tasks/page.tsx` | 15-20 | 可前端固定，无需 API | - |
| `initialTasks` | `src/app/(dashboard)/projects/[id]/tasks/page.tsx` | 22-30 | `GET /projects/{projectId}/tasks` | 待对接 |
| 任务创建表单 | 同上 | 149-258 | `POST /projects/{projectId}/tasks` | 待对接 |
| 任务拖拽移动 | 同上 | 270-304 | `POST /projects/{projectId}/tasks/{id}/move` | 待对接 |

### 2. 任务详情模块

| Mock 数据 | 所在文件 | 行号 | 对应后端接口 | 接口状态 |
|-----------|----------|------|--------------|----------|
| `mockTask` | `src/app/(dashboard)/tasks/[id]/page.tsx` | 29-65 | `GET /projects/{projectId}/tasks/{id}` | 待对接 |
| 子任务列表 | 同上 | 40-45 | `GET /projects/{projectId}/tasks/{id}/subtasks` | 待对接 |
| 评论列表 | 同上 | 46-59 | `GET /projects/{projectId}/tasks/{id}/comments` | 待对接 |
| 活动历史 | 同上 | 60-64 | 暂无对应接口 | 需确认 |
| 切换完成状态 | 同上 | - | `POST /projects/{projectId}/tasks/{id}/toggle-complete` | 待对接 |

### 3. 消息中心模块

| Mock 数据 | 所在文件 | 行号 | 对应后端接口 | 接口状态 |
|-----------|----------|------|--------------|----------|
| `mockNotifications` | `src/app/(dashboard)/messages/page.tsx` | 26-93 | `GET /notifications` | 待对接 |
| 未读通知筛选 | 同上 | 129 | `GET /notifications/unread` | 待对接 |
| 标记已读 | 同上 | 151-156 | `POST /notifications/{id}/read` | 待对接 |
| 全部已读 | 同上 | 159-162 | `POST /notifications/read-all` | 待对接 |
| 收藏功能 | 同上 | 165-171 | 暂无对应接口 | 需确认 |
| 删除通知 | 同上 | 174-177 | `DELETE /notifications/{id}` | 待对接 |

### 4. 个人设置模块

| Mock 数据 | 所在文件 | 行号 | 对应后端接口 | 接口状态 |
|-----------|----------|------|--------------|----------|
| `mockUser` | `src/app/(dashboard)/settings/page.tsx` | 21-30 | `GET /user/profile` | ✅ 已有接口 |
| 更新个人资料 | 同上 | 56-68 | `PUT /user/profile` | 待对接 |
| 修改密码 | 同上 | 71-89 | `PUT /user/password` | 需确认 |
| 头像上传 | 同上 | 45-53 | `PUT /user/avatar` | 待对接 |
| 通知设置 | 同上 | 353-429 | 暂无对应接口 | 需确认 |

### 5. 仪表盘模块

| Mock 数据 | 所在文件 | 行号 | 对应后端接口 | 接口状态 |
|-----------|----------|------|--------------|----------|
| `metricsData` | `src/app/(dashboard)/dashboard/page.tsx` | 100-141 | `GET /dashboard/metrics` | 需确认 |
| `projectsData` | 同上 | 151-200 | 可复用 `GET /projects` | 待对接 |
| `tasksData` | 同上 | 210-251 | `GET /tasks/my` 或 `GET /tasks/todo` | 需确认 |
| `activitiesData` | 同上 | 253-294 | `GET /activities/recent` | 需确认 |
| `chartData` | 同上 | 296-304 | `GET /reports/weekly-trend` | 需确认 |

### 6. 项目详情页模块

| Mock 数据 | 所在文件 | 行号 | 对应后端接口 | 接口状态 |
|-----------|----------|------|--------------|----------|
| `mockStories` | `src/app/(dashboard)/projects/[id]/page.tsx` | 200-204 | `GET /projects/{projectId}/stories` | 待对接 |
| `mockTasks` | 同上 | 206-223 | `GET /projects/{projectId}/tasks` | 待对接 |
| `mockIssues` | 同上 | 226-230 | `GET /projects/{projectId}/issues` | 待对接 |

### 7. 成员管理模块 (部分完成)

| Mock 数据 | 所在文件 | 行号 | 对应后端接口 | 接口状态 |
|-----------|----------|------|--------------|----------|
| `initialMembers` | `src/app/(dashboard)/projects/[id]/members/page.tsx` | 13-18 | `GET /projects/{projectId}/members` | 待对接 |
| 编辑角色 | 同上 | 78-102 | `PUT /projects/{projectId}/members/{userId}` | 已对接 |
| 移除成员 | 同上 | 105-123 | `DELETE /projects/{projectId}/members/{userId}` | 已对接 |
| 添加成员 | 同上 | 49-75 | `POST /projects/{projectId}/members` | 已对接 |

---

## API 文件清单

| 文件路径 | 状态 | 说明 |
|----------|------|------|
| `src/lib/api/axios.ts` | ✅ 已完成 | HTTP 客户端封装 |
| `src/lib/api/endpoints.ts` | ✅ 已完成 | 端点常量定义 |
| `src/lib/api/project.ts` | ✅ 已完成 | 项目管理 API |
| `src/lib/api/comment.ts` | ✅ 已定义 | 评论 API (未使用) |
| `src/lib/api/epic.ts` | ✅ 已定义 | 史诗 API (未使用) |
| `src/lib/api/story.ts` | ✅ 已定义 | 用户故事 API (未使用) |
| `src/lib/api/issue.ts` | ✅ 已定义 | 问题追踪 API (未使用) |
| `src/lib/api/wiki.ts` | ✅ 已定义 | Wiki API (未使用) |
| `src/lib/api/report.ts` | ✅ 已定义 | 报表 API (未使用) |
| `src/lib/api/task.ts` | ❌ 不存在 | 需要创建 |

---

## P0 阶段：核心功能联调

### 任务 1: 任务看板页面接口对接

| 项目 | 内容 |
|------|------|
| **页面路径** | `src/app/(dashboard)/projects/[id]/tasks/page.tsx` |
| **优先级** | P0 |
| **涉及接口** | `GET /projects/{projectId}/tasks` - 获取任务列表<br>`POST /projects/{projectId}/tasks` - 创建任务<br>`PUT /projects/{projectId}/tasks/{id}` - 更新任务<br>`POST /projects/{projectId}/tasks/{id}/move` - 移动任务<br>`DELETE /projects/{projectId}/tasks/{id}` - 删除任务 |
| **需替换的 Mock** | `initialTasks` (行 22-30) |
| **需创建文件** | `src/lib/api/task.ts` - 任务相关 API 函数 |
| **需更新文件** | `src/lib/api/endpoints.ts` - 确认任务端点定义正确<br>`src/app/(dashboard)/projects/[id]/tasks/page.tsx` - 替换 Mock 数据为 API 调用<br>`src/types/task.ts` - 确认类型定义匹配后端响应 |
| **验证要点** | 1. 任务列表正确加载<br>2. 创建任务功能<br>3. 拖拽移动任务<br>4. 删除任务 |

### 任务 2: 任务详情页面接口对接

| 项目 | 内容 |
|------|------|
| **页面路径** | `src/app/(dashboard)/tasks/[id]/page.tsx` |
| **优先级** | P0 |
| **涉及接口** | `GET /projects/{projectId}/tasks/{id}` - 获取任务详情<br>`GET /projects/{projectId}/tasks/{id}/subtasks` - 获取子任务<br>`GET /projects/{projectId}/tasks/{id}/comments` - 获取评论<br>`POST /projects/{projectId}/tasks/{id}/toggle-complete` - 切换完成状态<br>`POST /projects/{projectId}/tasks/{id}/subtasks` - 添加子任务<br>`DELETE /projects/{projectId}/tasks/{id}/subtasks/{subtaskId}` - 删除子任务<br>`POST /projects/{projectId}/tasks/{id}/comments` - 添加评论 |
| **需替换的 Mock** | `mockTask` (行 29-65) |
| **需更新文件** | `src/lib/api/task.ts` - 添加任务详情、子任务相关 API<br>`src/lib/api/comment.ts` - 添加评论相关 API<br>`src/app/(dashboard)/tasks/[id]/page.tsx` - 替换 Mock 数据为 API 调用<br>`src/types/task.ts` - 确认类型定义<br>`src/types/comment.ts` - 确认类型定义 |
| **验证要点** | 1. 任务详情正确显示<br>2. 子任务 CRUD<br>3. 评论 CRUD<br>4. 状态切换 |

### 任务 3: 通知模块接口对接

| 项目 | 内容 |
|------|------|
| **页面路径** | `src/app/(dashboard)/messages/page.tsx` |
| **优先级** | P0 |
| **涉及接口** | `GET /notifications` - 获取通知列表<br>`GET /notifications/unread` - 获取未读通知<br>`POST /notifications/{id}/read` - 标记已读<br>`POST /notifications/read-all` - 全部已读<br>`DELETE /notifications/{id}` - 删除通知 |
| **需替换的 Mock** | `mockNotifications` (行 26-93) |
| **需创建文件** | `src/lib/api/notification.ts` - 通知相关 API 函数 |
| **需更新文件** | `src/app/(dashboard)/messages/page.tsx` - 替换 Mock 数据为 API 调用<br>`src/types/notification.ts` - 可能需要创建或确认通知类型定义 |
| **验证要点** | 1. 通知列表加载<br>2. 未读筛选<br>3. 标记已读<br>4. 全部已读<br>5. 删除通知 |

### 任务 4: 个人设置页面接口对接

| 项目 | 内容 |
|------|------|
| **页面路径** | `src/app/(dashboard)/settings/page.tsx` |
| **优先级** | P0 |
| **涉及接口** | `GET /user/profile` - 获取用户信息 (已对接)<br>`PUT /user/profile` - 更新用户信息<br>`PUT /user/avatar` - 上传头像<br>`PUT /user/password` - 修改密码 (需确认接口) |
| **需替换的 Mock** | `mockUser` (行 21-30) |
| **相关文件** | `src/types/user.ts`<br>`src/lib/api/user.ts` (需创建) |
| **验证要点** | 1. 用户信息加载<br>2. 更新用户信息<br>3. 上传头像<br>4. 修改密码 |

### 任务 5: 成员管理页面完善

| 项目 | 内容 |
|------|------|
| **页面路径** | `src/app/(dashboard)/projects/[id]/members/page.tsx` |
| **优先级** | P0 |
| **涉及接口** | `GET /projects/{projectId}/members` - 获取成员列表 (待对接)<br>`POST /projects/{projectId}/members` - 添加成员 (已对接)<br>`PUT /projects/{projectId}/members/{userId}` - 更新角色 (已对接)<br>`DELETE /projects/{projectId}/members/{userId}` - 移除成员 (已对接) |
| **需替换的 Mock** | `initialMembers` (行 13-18) |
| **需更新文件** | `src/app/(dashboard)/projects/[id]/members/page.tsx` - 替换 Mock 初始数据为 API 调用 |
| **验证要点** | 1. 成员列表加载<br>2. 添加成员<br>3. 编辑角色<br>4. 移除成员 |

---

## P1 阶段：仪表盘优化

### 任务 6: 仪表盘页面接口对接

| 项目 | 内容 |
|------|------|
| **页面路径** | `src/app/(dashboard)/dashboard/page.tsx` |
| **优先级** | P1 |
| **涉及接口** | `GET /dashboard/metrics` - 关键指标 (需确认)<br>`GET /projects` - 项目概览 (可复用现有接口)<br>`GET /tasks/my` - 我的待办任务 (需确认)<br>`GET /activities/recent` - 团队动态 (需确认)<br>`GET /reports/weekly-trend` - 本周趋势 (需确认) |
| **需替换的 Mock** | `metricsData` (行 100-141)<br>`projectsData` (行 151-200)<br>`tasksData` (行 210-251)<br>`activitiesData` (行 253-294)<br>`chartData` (行 296-304) |
| **需确认事项** | 后端是否提供仪表盘聚合接口 |
| **验证要点** | 1. 指标卡片数据<br>2. 项目列表<br>3. 待办任务<br>4. 团队动态<br>5. 趋势图表 |

---

## P2 阶段：独立功能模块

### 任务 7: 项目详情页数据对接优化

| 项目 | 内容 |
|------|------|
| **页面路径** | `src/app/(dashboard)/projects/[id]/page.tsx` |
| **优先级** | P2 |
| **涉及接口** | `GET /projects/{projectId}/epics` - 获取史诗列表<br>`GET /projects/{projectId}/stories` - 获取故事列表<br>`GET /projects/{projectId}/issues` - 获取问题列表 |
| **需替换的 Mock** | `mockStories` (行 200-204)<br>`mockTasks` (行 206-223)<br>`mockIssues` (行 226-230) |
| **相关文件** | `src/lib/api/epic.ts` (API 已定义)<br>`src/lib/api/story.ts` (API 已定义)<br>`src/lib/api/issue.ts` (API 已定义) |

### 任务 8: 史诗管理功能开发

| 项目 | 内容 |
|------|------|
| **页面** | 待创建 |
| **优先级** | P2 |
| **涉及接口** | `GET /projects/{projectId}/epics`<br>`POST /projects/{projectId}/epics`<br>`PUT /projects/{projectId}/epics/{id}`<br>`DELETE /projects/{projectId}/epics/{id}` |
| **相关文件** | `src/lib/api/epic.ts` (API 已定义)<br>`src/types/epic.ts` |

### 任务 9: 用户故事管理功能开发

| 项目 | 内容 |
|------|------|
| **页面** | 待创建 |
| **优先级** | P2 |
| **涉及接口** | `GET /projects/{projectId}/stories`<br>`POST /projects/{projectId}/stories`<br>`PUT /projects/{projectId}/stories/{storyId}`<br>`DELETE /projects/{projectId}/stories/{storyId}` |
| **相关文件** | `src/lib/api/story.ts` (API 已定义)<br>`src/types/story.ts` |

### 任务 10: 问题追踪功能开发

| 项目 | 内容 |
|------|------|
| **页面** | 待创建 |
| **优先级** | P2 |
| **涉及接口** | `GET /projects/{projectId}/issues`<br>`POST /projects/{projectId}/issues`<br>`PUT /projects/{projectId}/issues/{issueId}`<br>`DELETE /projects/{projectId}/issues/{issueId}` |
| **相关文件** | `src/lib/api/issue.ts` (API 已定义)<br>`src/types/issue.ts` |

### 任务 11: Wiki 文档功能开发

| 项目 | 内容 |
|------|------|
| **页面** | 待创建 |
| **优先级** | P2 |
| **涉及接口** | `GET /projects/{projectId}/wiki`<br>`POST /projects/{projectId}/wiki`<br>`PUT /projects/{projectId}/wiki/{docId}`<br>`DELETE /projects/{projectId}/wiki/{docId}` |
| **相关文件** | `src/lib/api/wiki.ts` (API 已定义)<br>`src/types/wiki.ts` |

### 任务 12: 报表功能开发

| 项目 | 内容 |
|------|------|
| **页面** | 待创建 |
| **优先级** | P2 |
| **涉及接口** | `GET /projects/{projectId}/reports/burndown` - 燃尽图<br>`GET /projects/{projectId}/reports/cumulative-flow` - 累积流程图<br>`GET /projects/{projectId}/reports/velocity` - 速率图 |
| **相关文件** | `src/lib/api/report.ts` (API 已定义)<br>`src/types/report.ts` |

---

## 任务状态总览

| 优先级 | 任务 | 状态 | 备注 |
|--------|------|------|------|
| P0 | 任务 1: 任务看板页面接口对接 | 待开始 | 需创建 task.ts |
| P0 | 任务 2: 任务详情页面接口对接 | 待开始 | 依赖任务 1 |
| P0 | 任务 3: 通知模块接口对接 | 待开始 | 需创建 notification.ts |
| P0 | 任务 4: 个人设置页面接口对接 | 待开始 | 需创建 user.ts |
| P0 | 任务 5: 成员管理页面完善 | 待开始 | 仅需替换 Mock 初始化 |
| P1 | 任务 6: 仪表盘页面接口对接 | 待开始 | 需确认后端接口 |
| P2 | 任务 7: 项目详情页数据对接优化 | 待开始 | API 已定义 |
| P2 | 任务 8: 史诗管理功能开发 | 待开始 | API 已定义，需创建页面 |
| P2 | 任务 9: 用户故事管理功能开发 | 待开始 | API 已定义，需创建页面 |
| P2 | 任务 10: 问题追踪功能开发 | 待开始 | API 已定义，需创建页面 |
| P2 | 任务 11: Wiki 文档功能开发 | 待开始 | API 已定义，需创建页面 |
| P2 | 任务 12: 报表功能开发 | 待开始 | API 已定义，需创建页面 |

---

## 执行流程

### 1. 接口替换
- 查看后端 Swagger 文档确认接口格式
- 替换 Mock 数据为真实 API 调用
- 更新类型定义（如需要）

### 2. 自测
- 启动开发服务器 `npm run dev`
- 测试页面功能完整性
- 检查控制台是否有错误
- 验证数据正确显示

### 3. 打包验证
- 运行 `npm run build`
- 确保构建无错误

### 4. Git 提交
```bash
git add .
git commit -m "feat: [页面名] 接口联调完成"
```

---

## 提交规范

所有任务完成后的提交格式：

```
feat: [页面/功能名称] 接口联调完成
```

---

## 待确认事项

### 后端接口确认
- [ ] 确认后端服务运行正常（端口 8080）
- [ ] 确认 CORS 配置正确
- [ ] 确认任务相关接口已实现并可访问
- [ ] 确认通知接口已实现并可访问

### 接口格式确认
- [ ] 任务列表响应格式
- [ ] 任务详情响应格式
- [ ] 通知响应格式
- [ ] 用户信息更新请求格式

### 缺失接口确认
- [ ] 活动历史接口（任务详情页）
- [ ] 收藏通知接口
- [ ] 通知设置接口
- [ ] 仪表盘聚合接口
- [ ] 修改密码接口

---

## 验证测试方案

### 任务 1 验证（任务看板）
1. 访问 `/projects/{id}/tasks` 页面
2. 验证任务列表正确加载
3. 验证创建任务功能
4. 验证拖拽移动任务功能
5. 验证编辑和删除任务功能
6. 检查浏览器控制台无错误

### 任务 2 验证（任务详情）
1. 点击任务卡片跳转到详情页
2. 验证任务详情正确显示
3. 验证子任务列表加载
4. 验证评论列表加载
5. 验证切换完成状态功能

### 任务 3 验证（通知模块）
1. 访问 `/messages` 页面
2. 验证通知列表加载
3. 验证未读通知筛选
4. 验证标记已读功能
5. 验证全部已读功能

### 任务 4 验证（个人设置）
1. 访问 `/settings` 页面
2. 验证用户信息加载
3. 验证更新用户信息功能
4. 验证上传头像功能（如后端支持）

### 任务 5 验证（成员管理）
1. 访问 `/projects/{id}/members` 页面
2. 验证成员列表加载
3. 验证添加成员功能
4. 验证编辑角色功能
5. 验证移除成员功能

### 任务 6 验证（仪表盘）
1. 访问 `/dashboard` 页面
2. 验证指标卡片数据
3. 验证项目概览列表
4. 验证待办任务列表
5. 验证团队动态

### 构建验证
每个任务完成后执行：
```bash
npm run build
```
确保无构建错误。